<nav class =" navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        <a href="/" class="navbar-brand">Test</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav"><span class="navbar-toggle-icon"></span></navbar-toggle-icon></button>
    
    <div class="collapse navbar-collapse" id="mobile-nav">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" class="nav-link">Home</a>
            </li>
            {% if app.user %}
            <li class="nav-item">
                <a href="/article/maker" class="nav-link">Article manager</a>
            </li>
            <li class="nav-item">
                    <a href="/category/maker" class="nav-link">Category manager</a>
            </li>
            {% endif %}
            {% if not app.user %}
            <li class="nav-item">
                <a href="{{ path('login') }}" class="nav-link">Log in</a>
            </li>
            {% endif %}
             {% if app.user %}
            <li class="nav-item">
                    <a href="{{ path('logout') }}" class="nav-link">Log out</a>
            </li>
            {% endif %}
        </ul>
        {% if app.user %}
    <div class="ml-4">
        <a><font color="white"><b>Welcome back, Admin!</b></font></a>
    </div>
    {% endif %}
    </div>
    </div>
    </nav>